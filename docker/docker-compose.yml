services:
  qingqiu-world:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: qingqiu-world
    ports:
      - "${PORT:-18888}:18888"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
